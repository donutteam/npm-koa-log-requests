{
  "name": "@donutteam/koa-log-requests",
  "version": "1.1.0",
  "description": "A class for creating Koa middlewares that log when requests start and end.",
  "main": "./lib/index.js",
  "scripts": {
    "build": "npx rimraf lib && npx tsc",
    "lint": "eslint . -c ./.eslintrc.cjs",
    "lint-fix": "eslint . -c ./.eslintrc.cjs --fix",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/donutteam/koa-log-requests.git"
  },
  "keywords": [
    "koa",
    "middleware",
    "logging"
  ],
  "author": "Donut Team",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "chalk": "5.0.1"
  },
  "devDependencies": {
    "@types/koa": "^2.13.6",
    "koa": "^2.13.4",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.3"
  }
}
